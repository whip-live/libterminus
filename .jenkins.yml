version: "2"

services:
  tests:
    image: evonove/ci
    command: tox -r --workdir=/tmp/.toxterminus-${RANDOM_WD}
    volumes_from:
      - container:jenkins-data
    environment:
      - WORKSPACE
